# MFC run artifacts (regenerated by validate.py) — keep only the cases, harness, figures, diagrams, summary
runs/
__pycache__/
cases/restart_data/
cases/*.inp
cases/*.dat
cases/*.inf
cases/MFC.out
cases/MFC.sh
