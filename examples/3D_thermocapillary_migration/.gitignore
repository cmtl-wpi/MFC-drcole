# Raw simulation output (large; reproducible via run_ygb.py). Keep results/ (JSON) + figures/.
runs/
__pycache__/
*.pyc
# In-place output if a case is ever run from this directory (it should not be -- use run_ygb.py)
restart_data/
D/
silo_hdf5/
viz/
*.inp
*.dat
MFC.out
MFC.sh
run_time.inf
smoke_run.log
